<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:CerfApp.Controls"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             ios:Page.UseSafeArea="True"
             x:Class="CerfApp.Cerfa13750VehiculePage"
             NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="Auto, *, Auto">

        <controls:HeaderView Grid.Row="0"
                             PageTitle="Certificat d'immatriculation (13750*07)"
                             ShowBack="True"
                             ShowSettings="False" />

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="16" Spacing="0">

                <Label Text="LE VÉHICULE"
                       FontAttributes="Bold" FontSize="15"
                       HorizontalOptions="Center" HorizontalTextAlignment="Center"
                       Margin="0,8,0,12" />

                <Label Text="(A)  Numéro d'immatriculation" Style="{StaticResource FieldLabel}" />
                <Entry x:Name="ImmatriculationEntry" Placeholder="AB-123-CD" Style="{StaticResource FieldEntry}" />

                <Grid ColumnDefinitions="*, *, *">
                    <VerticalStackLayout Grid.Column="0" Spacing="0">
                        <Label Text="(B)  Mise en circulation" Style="{StaticResource FieldLabel}" />
                        <Entry x:Name="MiseCircEntry" Placeholder="JJ/MM/AAAA" Keyboard="Numeric" Style="{StaticResource FieldEntry}" />
                    </VerticalStackLayout>
                    <VerticalStackLayout Grid.Column="1" Spacing="0">
                        <Label Text="(I)  Date certificat" Style="{StaticResource FieldLabel}" />
                        <Entry x:Name="CertEntry" Placeholder="JJ/MM/AAAA" Keyboard="Numeric" Style="{StaticResource FieldEntry}" />
                    </VerticalStackLayout>
                    <VerticalStackLayout Grid.Column="2" Spacing="0">
                        <Label Text="Date d'entrée" Style="{StaticResource FieldLabel}" />
                        <Entry x:Name="DateEntreeEntry" Placeholder="JJ/MM/AAAA" Keyboard="Numeric" Style="{StaticResource FieldEntry}" />
                    </VerticalStackLayout>
                </Grid>

                <Label Text="Numéro de formule" Style="{StaticResource FieldLabel}" />
                <Entry x:Name="NumFormuleEntry" Placeholder="ex : 2025AE62234" Style="{StaticResource FieldEntry}" />

                <Label Text="(D.1)  Marque" Style="{StaticResource FieldLabel}" />
                <Entry x:Name="MarqueEntry" Placeholder="ex : RENAULT" Style="{StaticResource FieldEntry}" />

                <Label Text="(D.2/D.3)  Type / Dénomination" Style="{StaticResource FieldLabel}" />
                <Entry x:Name="ModeleEntry" Placeholder="ex : CLIO IV" Style="{StaticResource FieldEntry}" />

                <Label Text="(D.2)  Type mine (TVV)" Style="{StaticResource FieldLabel}" />
                <Entry x:Name="TypeMineEntry" Placeholder="ex : AA0BP0KAA" Style="{StaticResource FieldEntry}" />

                <Label Text="(E)  Numéro d'identification (VIN)" Style="{StaticResource FieldLabel}" />
                <Entry x:Name="SerieEntry" Placeholder="17 caractères" Style="{StaticResource FieldEntry}" />

                <Label Text="(J.1)  Genre national" Style="{StaticResource FieldLabel}" />
                <Entry x:Name="GenreEntry" Placeholder="ex : VP, CTTE…" Style="{StaticResource FieldEntry}" />

                <Label Text="N° d'exploitation" Style="{StaticResource FieldLabel}" />
                <Entry x:Name="NumExploitEntry" Placeholder="Optionnel" Style="{StaticResource FieldEntry}" />

                <Label Text="Couleur dominante" Style="{StaticResource FieldLabel}" />
                <Picker x:Name="CouleurPicker"
                        Title="Choisir une couleur"
                        SelectedIndexChanged="OnCouleurChanged"
                        Margin="0,4,0,8" />

                <Label Text="Teinte" Style="{StaticResource FieldLabel}" />
                <HorizontalStackLayout Spacing="16" Margin="0,4,0,8">
                    <RadioButton x:Name="TeinteNonRadio"   Content="Non précisée" GroupName="GrpTeinte" />
                    <RadioButton x:Name="TeinteCLairRadio"  Content="Clair"        GroupName="GrpTeinte" />
                    <RadioButton x:Name="TeinteFonceRadio"  Content="Foncé"        GroupName="GrpTeinte" />
                </HorizontalStackLayout>

            </VerticalStackLayout>
        </ScrollView>

        <Grid Grid.Row="2" Style="{StaticResource FooterGrid}"
              ColumnDefinitions="*, *">

            <Button Grid.Column="0"
                    Text="Scanner"
                    Clicked="OnScanClicked" />

            <Button Grid.Column="1"
                    Text="Suivant"
                    Clicked="OnSuivantClicked" />

        </Grid>

    </Grid>

</ContentPage>
